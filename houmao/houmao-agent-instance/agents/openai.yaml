interface:
  display_name: "Houmao Manage Agent Instance"
  short_description: "Manage live Houmao agent instances."
  default_prompt: "Use $houmao-agent-instance to launch, join, list, stop, relaunch, or clean up Houmao managed-agent instances in this workspace, including launches from direct selectors or launch dossiers."
