interface:
  display_name: "Houmao Agent Inspect"
  short_description: "Inspect Houmao managed agents through read-only supported surfaces."
  default_prompt: "Use $houmao-agent-inspect to inspect Houmao managed agents through supported read-only surfaces such as state, detail, mailbox posture, logs, runtime artifacts, and tmux capture once the exact session name or pane target is identified."
