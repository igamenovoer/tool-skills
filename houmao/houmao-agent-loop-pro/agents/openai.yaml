interface:
  display_name: "Houmao Agent Loop Pro"
  short_description: "Author and operate topology-aware generated execplans for tree-loop or generic-loop Houmao loops."
  default_prompt: "Use $houmao-agent-loop-pro only when explicitly requested or when the user names a supported pro loop operation. If no operation or prompt is given, run init: ask for <loop-dir>, then scaffold intention files including project-context.md. For details, read SKILL.md and the routed subskill; use packaged scaffold templates and route Houmao platform operations to their maintained skills."
