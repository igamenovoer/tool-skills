# Objective Specs

## Purpose

This directory contains generated objective, constraint, policy, and success-posture contracts.

## Contents

- `objective.toml`: generated objective items when emitted.
- `policy.toml`: generated objective policy values or references when emitted.
- additional generated files: objective-specific contracts indexed by `../../manifest.toml`.
