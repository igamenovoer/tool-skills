# Workspace Specs

## Purpose

This directory contains generated workspace contracts for agent work roots, shared resources, and workspace-manager inputs.

## Contents

- `workspace.toml`: generated workspace policy when emitted.
- additional generated files: workspace-specific contracts indexed by `../../manifest.toml`.
