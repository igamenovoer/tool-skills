# Validation

This starter document should be completed from the current generated artifacts during finalization.

## Baseline Checks

- Confirm `../manifest.toml` indexes the emitted artifacts for this revision.
- Confirm canonical generated paths under `../specs/`, `../skills/`, `../agents/`, and `../harness/`.
- Record intentional omissions or accepted equivalents.
- Summarize any harness self-checks or validation commands when present.

## Revision

- Scaffolded on `${generated_date}` with plan revision `${plan_revision}`.
