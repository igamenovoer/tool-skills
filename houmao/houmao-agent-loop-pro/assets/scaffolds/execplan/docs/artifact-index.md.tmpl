# Artifact Index

This starter document should be completed from the current generated artifacts during finalization.

## Package Areas

- `../specs/`: generated contracts for objective, collaboration, communication, state, workspace, run, and participants.
- `../harness/`: loop-local commands for validation, lookup, rendering, query, and controlled apply.
- `../skills/`: generated flat skill packages for event, tick, operator, or shared responsibilities.
- `../agents/`: concrete agent bindings, profile material, and notifier prompt text.

## Notes

- Replace placeholder summaries with the actual emitted files for this revision.
- Record intentional omissions here and in `../manifest.toml`.
