# Launch Dossiers Have Moved

This file is retained only as a routing note for older installed copies.

Use `houmao-agent-definition` subcommand `launch-dossiers` for explicit recipe-backed launch-profile authoring through `internals native-agent launch-dossiers list|get|add|set|remove`.

Current local route: `houmao-agent-definition/subskills/low-level/launch-dossiers.md`.

Do not run launch-profile authoring from `houmao-project-mgr`.
